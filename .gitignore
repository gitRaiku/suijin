# Get rid of what cache is left over
*cache*

# Get rid of everything generated by autoconf
config*
aclocal.m4
Makefile*
compile
depcomp
install-sh
missing
stamp-h1

# Keep the autoconf config files
!Makefile.am
!configure.ac

# Get rid of the resulting binary
suijin

# Get rid of leftover object files
*.o
src/.deps
shaders/*.spv
logout

# Get rid of the gdb history
.gdb_history
